This project depends on node.js, mongodb and webpack.

After installing these dependencies, you need to run:

npm install

to install the project dependencies. Then you need to run:

webpack

to build the project. And to run the web server:

npm start
